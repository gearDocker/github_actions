name: First Workflow

on:
  - workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Display title
        run: echo "Awesome title"
      - name: Display Node
        run: |
          node -v
          npm -v
      - name: Display OS info
        run: |
          uname -a
